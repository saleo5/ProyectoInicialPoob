/**
 * Representa un agujero que aparece visualmente cuando el demonio permite el paso.
 * No absorbe partículas ni las elimina.
 */
public class Hole {
    private int x, y;
    private int capacidad;
    private int ocupados = 0;
    private boolean full = false;
    private Circle hole;

    /**
     * Constructor del agujero.
     * @param x posición en X del centro
     * @param y posición en Y del centro
     * @param capacidad (actualmente no se usa, pero se conserva por compatibilidad)
     */
    public Hole(int x, int y, int capacidad) {
        this.hole = new Circle(x - 10, y - 10, 20, "#01ffdc"); // color celeste
        this.x = x;
        this.y = y;
        this.capacidad = capacidad;
    }

    /**
     * Muestra el agujero visualmente .
     */
    public void makeVisible() {
        this.hole.makeVisible();
    }

    
}
